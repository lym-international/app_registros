/*import { formatDate } from '@angular/common';
export class AllActionsModel {
  //id: string;
  //title: string;
  //category: string;
  date: string;
  //endDate: string;
  //details: string;

  constructor(AllActions: AllActionsModel) {
    {
      //this.id = AllActions.id || '';
      //this.title = AllActions.title || '';
      //this.category = AllActions.category || '';
      this.date = formatDate(new Date(), 'yyyy-MM-dd', 'en') || '';
      //this.endDate = formatDate(new Date(), 'yyyy-MM-dd', 'en') || '';
      //this.details = AllActions.details || '';
    }
  }
  public getRandomID(): number {
    const S4 = () => {
      return ((1 + Math.random()) * 0x10000) | 0;
    };
    return S4() + S4();
  }
}*/